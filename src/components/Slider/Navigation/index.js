import React from 'react';
import './navigation.css';
import PropTypes from 'prop-types';

const Navigation = ({ slider, onClickNavSlide, onClickNavPrev, onClickNavNext }) => {
  console.log('Navigation slider: ', slider);
  return (
    <div>
      Navigation
    </div>
  );
}

Navigation.propTypes = {}

export default Navigation;
