import React from 'react';
import './thumbnail.css';
import PropTypes from 'prop-types';

const Thumbnail = ({ slider, onClickThumb }) => {
  console.log('Thumbnail slider: ', slider);
  return (
    <div>
      Thumbnail
    </div>
  );
}

Thumbnail.propTypes = {}

export default Thumbnail;
